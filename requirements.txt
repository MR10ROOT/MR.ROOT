# ============================================================
# MR.ROOT — NetHunter Scanner v3.9
# Instalacja: pip install -r requirements.txt --break-system-packages
# ============================================================

# === Wymagane narzędzia systemowe (apt / pkg) ===
# apt install nmap exploitdb sqlmap adb -y          (Kali Linux)
# pkg install nmap sqlmap android-tools             (Termux/NetHunter)

# === Core — skanowanie sieci ===
python-nmap>=0.7.1

# === v3.9 — Passive Packet Sniffer (sniff_scan) ===
# Wymaga też tshark w systemie: apt install tshark
scapy>=2.5.0

# === v3.8 — Frida SSL Unpinning przez ADB ===
# WAŻNE: wersja musi być identyczna z frida-server na urządzeniu!
# Sprawdź: frida --version
# Pobierz matching frida-server: github.com/frida/frida/releases
# Dostępne wersje pip: max 14.8.2 (17.x tylko jako wheel z github)
frida-tools==14.8.2

# === v3.6 — Headless browser scan (komenda: hw <URL>) ===
# Po instalacji uruchom: playwright install chromium
playwright>=1.40.0

# === Parsowanie konfiguracji (config.yaml) ===
pyyaml>=6.0

# ============================================================
# Stdlib Python 3.10+ (nie wymagają instalacji):
# json, socket, ssl, ipaddress, subprocess, urllib, argparse,
# hashlib, concurrent.futures, threading, tempfile, re, shutil,
# random, os, sys, time, datetime, collections
# ============================================================
